{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Model Router Configuration",
  "description": "Configuration for the Claude model router hook. Controls keyword/pattern matching for routing prompts to opus, sonnet, or haiku.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to the JSON schema file for IDE validation and autocomplete."
    },
    "opus": {
      "$ref": "#/definitions/tierConfig",
      "description": "Configuration for opus-tier routing. Uses keyword matching and regex patterns."
    },
    "sonnet": {
      "$ref": "#/definitions/tierConfig",
      "description": "Configuration for sonnet-tier routing. Uses regex patterns."
    },
    "haiku": {
      "$ref": "#/definitions/tierConfig",
      "description": "Configuration for haiku-tier routing. Uses regex patterns."
    },
    "fallback": {
      "type": "object",
      "description": "AI-powered fallback classification when regex matching produces no result. Uses the claude CLI with Haiku to classify ambiguous prompts.",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable Haiku-based fallback classification. When true, prompts that don't match any regex pattern are classified by Haiku via the claude CLI."
        }
      }
    }
  },
  "definitions": {
    "tierConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["extend", "replace"],
          "default": "extend",
          "description": "\"extend\" merges with built-in defaults; \"replace\" discards defaults entirely."
        },
        "keywords": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Plain-text keywords to match against the prompt (case-insensitive)."
        },
        "patterns": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Regex patterns to match against the prompt."
        },
        "remove_keywords": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Keywords to remove from the defaults (only used in \"extend\" mode)."
        },
        "remove_patterns": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "description": "Patterns to remove from the defaults (only used in \"extend\" mode)."
        }
      }
    }
  }
}
